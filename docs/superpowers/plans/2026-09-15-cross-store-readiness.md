# Cross-store readiness fixes

Source requirements: approved Apple and Google audits, user request to fix both platforms. Baseline eed0214. Preserve native pairing, current signing identity, actor isolation, 7-day deletion promise, and iOS behavior. No deployment or store submission. Vendor and physical-device evidence must remain unverified until obtained.

1. Shared privacy and permission UX: accurate Android just-in-time scan disclosure and denial recovery, shared notice, explicit public account deletion instructions independent of app installation. Tests cover consent gating; no changes to native permission list here.
2. Android native containment and SDK data hygiene: guard scan/stop/adapter callbacks, contain stale lifecycle work; investigate supported logging suppression and cancellation. Only implement safe suppression/cleanup. Never clear export exclusion without terminal evidence. Add focused native tests where feasible.
3. Account deletion operations: add overdue visibility and evidence-focused operator runbook; preserve idempotency and external evidence requirement. Implement safe-origin enrollment validation with existing production/custom-scheme/local-development compatibility. Do not invent retention commitments or provider APIs.
4. Android release foundation: remove unused release permissions, constrain affected Java dependencies, explicit store build/verification commands with signing, manifest, origin, native alignment and readiness gates. Check current first-party vendor evidence. Do not patch proprietary binaries or call an incompatible binary compliant.
5. Review and verification: shared tests/build, release-negative tests, native compile, inspect generated manifests; update combined submission checklist with fixed versus externally blocked items and exact evidence. Review each task plus full diff, resolve findings. Keep branch local.

Ownership: task 1 shared product content, legal/privacy/setup UI; task 2 Kotlin bridge; task 3 account-deletion operator/service and enrollment-link parser; task 4 app.config/plugins, Gradle dependencies, release scripts; task 5 root tests/docs and reviewer fixes. Shared interfaces: permission disclosure precedes existing controller startScan; native permission set stays SDK-compatible; release manifest verifier reads generated merged manifest; deletion operator consumes existing request state and evidence.
